version: "3.9"

services:
  app:
    image: appdev
    container_name: development
    env_file: ../.env

    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile

    ports:
      - 8080:80
    
    volumes:
      - ..:/app
